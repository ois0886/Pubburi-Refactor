<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pubburi.pub.model.dao.OrderDetailDao">

    <!-- 상세 주문 추가 -->
    <insert id="insert" parameterType="OrderDetail" useGeneratedKeys="true" keyProperty="dId">
        INSERT INTO t_order_detail (order_id, product_id, quantity)
        VALUES (#{orderId}, #{productId}, #{quantity})
    </insert>

    <!-- 주문 ID로 상세 조회 -->
    <select id="selectByOrderId" parameterType="int" resultType="OrderDetail">
        SELECT * FROM t_order_detail WHERE order_id = #{orderId}
    </select>

    <!-- 주문 ID로 상세 삭제 -->
    <delete id="deleteByOrderId" parameterType="int">
        DELETE FROM t_order_detail WHERE order_id = #{orderId}
    </delete>

</mapper>
